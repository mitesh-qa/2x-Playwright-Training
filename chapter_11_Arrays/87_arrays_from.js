let numbers = Array.from(123456)
console.log(numbers); //[] empty

let name = Array.from("Mitesh")
console.log(name); 

let word = "Hello";
let ArrWord = Array.from(word.toUpperCase());
console.log(ArrWord);